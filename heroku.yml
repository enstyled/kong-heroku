build:
  config:
    KONG_TAG: latest
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - ./bootstrap && kong migrations bootstrap
run:
  web:
    image: web
    command:
      - ./bootstrap && ./docker-entrypoint.sh kong docker-start