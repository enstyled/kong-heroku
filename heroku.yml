build:
  config:
    KONG_TAG: latest
  docker:
    kong: Dockerfile
release:
  image: kong
  command: ./bootstrap && kong migrations bootstrap
run:
  web:
    image: kong
    command: ./bootstrap && kong kong docker-start