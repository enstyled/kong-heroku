build:
  config:
    KONG_TAG: ubuntu
  docker:
    kong: Dockerfile
release:
  image: kong
  command:
    - ./bootstrap && kong migrations bootstrap
run:
  web:
    image: kong
    command:
      - ./bootstrap && ./docker-entrypoint.sh kong docker-start